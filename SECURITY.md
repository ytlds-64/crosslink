# 安全说明（CrossLink）

## 服务端身份私钥（`crosslink-server.key`）

CrossLink 的服务端握手使用**静态 X25519 身份密钥**，其公钥指纹用于客户端的 pinning / TOFU 信任。

- 文件：`crosslink-server.key`（**32 字节原始私钥**）
- **绝不要提交到 git**（已在 `.gitignore` 排除 `*.key` / `crosslink-server.key`）
- **绝不要明文打包 / 通过普通聊天 / 邮件 / 截图分享**
- 若已泄露：删除旧 key，重新生成，并通知所有客户端重新确认指纹

### 泄露处理
1. 立即视为该身份已不可信。
2. 服务端停止使用该 key，删除文件。
3. 客户端侧清除已 pin 的指纹（TOFU 缓存），重新完成首次信任。

## 信任模型
- 服务端身份 = 静态公钥 SHA-256 指纹（冒号十六进制，如 `AB:CD:EF:...:12`）。
- 客户端首次连接打印对端指纹；建议用 `--fingerprint <FP>` 锁定，避免中间人。
- 未提供 `--fingerprint` 时采用 TOFU（信任首次），仅限受信任局域网调试。

## 其他
- 加密通道为 Noise 式 X25519 + AES-256-GCM 自研握手（非完整 TLS），安全性依赖指纹 pinning 的正确使用。
- 传输层未做前向保密之外的额外加固；跨不可信网络请配合 VPN / 隧道。

# CrossLink M3 双机验证指南（Windows 服务端 + macOS 客户端）

> 适用：你手头是 **1 台 Windows + 1 台 Mac**（恰好是跨平台真机组合，比两台 Windows 更有价值）。  
> 目标：验证 M3 边缘切换 / 指针漫游——光标从左屏右缘推出 → 落到 Mac，反向再跳回 Windows；**全程不转发键鼠**。

---

## 0. 为什么这次 Mac 端很轻松

M3 的 `--switch` 模式在**两端都不启动捕获（CGEventTap）和注入（SendInput / CGEventPost）**。  
两端只做四件事：

1. 交换屏幕几何（`Hello` 里的 `screen_w/h`）
2. 轮询本机光标位置（`get_cursor_pos`）
3. 收到 `Transfer` 时把光标 warp 到接缝边（`set_cursor_pos`）
4. 心跳（RTT）

因此 macOS 端只要「读光标」（无需授权）+「warp 光标」（可能需要辅助功能授权）即可。  
**直接复用你 M2.4 已建好的 `.app bundle`（已获辅助功能授权），无需再配输入监控。**

---

## 1. 拓扑与默认布局

```
┌──────────────┐     接缝      ┌──────────────┐
│  Windows     │ ───────────▶ │    macOS     │
│  (server)    │  Transfer    │   (client)   │
│  owner 初始  │              │  泊在左接缝  │
└──────────────┘              └──────────────┘
   左                             右
```

- 默认 `--side`：server=`Right`（对端在右），client=`Left`（对端在左）→ 水平拼接，Mac 在右。
- 若你物理上 **Mac 在 Windows 左边**，两端 `--side` 对调（见第 4 节）。

---

## 2. Windows 端（server / 初始 owner）

1. **构建**：沙箱里我已帮你 build 好 `target/x86_64-pc-windows-gnu/release/crosslink.exe`（就在你机器磁盘上）。  
   真机自己构建也行：
   ```powershell
   cargo build --release --target x86_64-pc-windows-gnu
   ```
2. **放置文件**（新建一个目录，如 `D:\crosslink\`），放这两样：
   - `crosslink.exe`
   - `crosslink-server.key`（仓库根目录已有，**必须同名同目录**——服务端身份私钥）
3. **放行防火墙**（管理员 PowerShell）：
   ```powershell
   New-NetFirewallRule -DisplayName "crosslink" -Direction Inbound -Program "D:\crosslink\crosslink.exe" -Action Allow -Profile Private
   ```
   或：Windows 安全中心 → 防火墙 → 允许应用通过 → 勾选 crosslink 的「专用」。
4. **启动 server**（在带显示器的真机桌面开一个终端）：
   ```powershell
   cd D:\crosslink
   .\crosslink.exe --server --switch --name win-srv
   ```
   终端会打印 `Server identity fingerprint ... AB:CD:EF:...`，**记下来**（首次可不加 `--fingerprint`，TOFU 自动信任）。
   > 这条机器就是你日常用的 Windows，物理键鼠由它原生接管（owner），crosslink 不碰你的输入。

---

## 3. macOS 端（client）

1. **拉最新代码并构建**（Mac 上）：
   ```zsh
   cd ~/WorkBuddy/2026-08-07-15-05-29/crosslink   # 你的本地仓库路径
   git pull
   cargo build --release
   ```

2. **覆盖进现有 .app**（保持 `com.crosslink.app` 不变，免重新授权）：
   ```zsh
   cp target/release/crosslink /Users/xiaohua/Applications/crosslink.app/Contents/MacOS/crosslink
   ```
   若 TCC 提示失效：系统设置 → 隐私与安全性 → 辅助功能 → 重新勾选 `crosslink.app` 一次。
3. **启动 client**（把 `<WIN_IP>` 换成 Windows 局域网 IP，如 `192.168.1.50`）：
   ```zsh
   /Users/xiaohua/Applications/crosslink.app/Contents/MacOS/crosslink \
     --client <WIN_IP> --switch --name mac-cli
   ```
   首次**不加** `--fingerprint`（TOFU 自动信任）；想固定信任时加 `--fingerprint <上面记的>`。  
   （也可直接用 `crosslink-m3-client.sh <WIN_IP>` 启动器。）

---

## 4. 成功现象 + 日志标记

### 看现象（最关键）

- [ ] 初始：鼠标在 Windows 上正常用（owner）。
- [ ] 把鼠标从 Windows 屏幕**右边缘**继续往右推 → 光标「跳」到 Mac 屏幕**左边缘**，随后在 Mac 上移动鼠标即可控制 Mac（Mac 变身 owner）。
- [ ] 在 Mac 上把鼠标推到**左边缘**往左 → 光标跳回 Windows 右边缘，Windows 重新接管。
- [ ] 全程**不转发**任何键鼠，延迟极低。

### 看日志（两端终端）

- 双方都出现：`handshake complete` / `peer hello: ... screen WxH` / `heartbeat ack, rtt=...`
- Windows 端：`switch: handed pointer to peer at (0, N)`
- Mac 端：`switch: pointer handed to me at (0, N)`
- 反向时：Mac 端 `switch: handed pointer to peer at (W-1, M)`；Windows 端 `switch: pointer handed to me at (W-1, M)`

---

## 5. 排错

- **连不上**：检查 Windows 防火墙是否放行 `crosslink.exe`（入站 / 专用网络）；确认两端同一局域网、IP 正确。
- **Mac 端光标不「落点」（warp 无效）**：多半是辅助功能授权未对 `.app` 生效。重新勾选 系统设置 → 隐私与安全性 → 辅助功能 → `crosslink.app`；或用裸二进制跑一次看是否报权限。
- **指纹不匹配**：首次连接别加 `--fingerprint`；若之前 TOFU 存了别的指纹，删除对应 known-peers 缓存后重来。
- **布局反了**（Mac 实际在 Windows 左边）：两端 `--side` 对调：
  ```powershell
  # Windows
  .\crosslink.exe --server --switch --side left --name win-srv
  ```
  ```zsh
  # macOS
  .../crosslink.app/Contents/MacOS/crosslink --client <WIN_IP> --switch --side right --name mac-cli
  ```

---

## 6. 安全提醒

- **不要在同一台机器上同时跑 server + client 做真实漫游**：两个进程抢同一个真实光标会打架。  
  同机只做冒烟请用 `tools/win-verify.ps1` 的 Test 3（loopback，不会抢真实光标）。
- `crosslink-server.key` 是服务端私钥，**不要**进 git、不要明文打包分享；client 端不需要它。
- 网络传输已加密（X25519 + AES-GCM），首次连接 TOFU 信任，建议二次连接用 `--fingerprint` 固定。

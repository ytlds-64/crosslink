# M2 双机验证指南（Win 主控 → Mac 被控）

> 目标：用 Windows 的键盘/鼠标**持续控制** Mac（传统软件 KVM / Synergy 式）。
> 这是 crosslink 的 **默认模式（M2 转发模式）**，不需要 `--switch`。

## 拓扑与模型

```
┌──────────────┐   TCP 4242 (X25519/AES-GCM)   ┌──────────────┐
│  Windows     │ ───────────────────────────▶  │    macOS     │
│  server      │     捕获 Win 键鼠 → 转发        │   client     │
│  (主控)      │                               │  (被控/注入) │
│  物理键鼠    │                               │  注入 Win 输入│
└──────────────┘                               └──────────────┘
```

- **单向 master→slave**：Windows（server）捕获本地键鼠 → 加密转发 → Mac（client）注入。
- Mac 的 `.app bundle` 需要**「输入监控」授权**（CGEventPost 注入所需）；辅助功能可选。
- Mac 自己的触控板/键盘在本地仍可用，只是它同时被 Win 的输入镜像驱动。
- 典型现象：**两个光标**——Win 屏上 Win 自己的光标，Mac 屏上被注入驱动的 Mac 光标，两者随 Win 鼠标同步移动（这是 M2 默认行为的已知特征，不是 bug）。

## 两端命令

### Windows（server / 主控，带显示器桌面）
```powershell
# 1) 防火墙放行（管理员 PowerShell 一次即可）
New-NetFirewallRule -DisplayName crosslink -Direction Inbound -Program "D:\crosslink\crosslink.exe" -Action Allow -Profile Private

# 2) 把 crosslink.exe 与仓库根目录的 crosslink-server.key 放同一目录（如 D:\crosslink\）
cd D:\crosslink
.\crosslink.exe --server --name win-srv
```
启动后应看到：
```
=== Server identity fingerprint ... ===
XX:XX:...:XX   ← 把这一串记下来
server listening on 0.0.0.0:4242
```

### macOS（client / 被控，复用 M2.4 的 .app bundle）
```zsh
# 先确认二进制是最新的（M2 是默认模式，M3 的 git pull 已包含 M2 代码，无需重新构建也能跑；如需重 build：）
cd ~/crosslink && cargo build --release
cp target/release/crosslink /Users/xiaohua/Applications/crosslink.app/Contents/MacOS/crosslink

# 直接用脚本（收 Windows 局域网 IP 作参数）：
zsh crosslink-m2-client.sh <Windows局域网IP>
# 等价于：crosslink --client <IP> --name mac-cli
```

## 验证通过标准

1. **握手 + 指纹比对**：Mac 端应打印 `handshake complete, server fingerprint = XX:XX:...:XX`，
   与 Windows 端那串**逐字节一致** → 回车接受（TOFU）。
2. **Win 键盘 → Mac**：在 Windows 上敲字符/快捷键，应在 **Mac 上**出现对应输入。
3. **Win 鼠标 → Mac**：移动 Windows 鼠标，Mac 屏上的光标应随之移动。
4. **日志标记**：
   - Windows 端：`server: keyboard+mouse capture → wire enabled`、`handshake complete`
   - Mac 端：`client: wire → SendInput inject enabled`（注：macOS 端日志写的是通用 inject 名）、`heartbeat ack`

## 排错

| 现象 | 可能原因 | 处理 |
|---|---|---|
| Mac 端 `handshake complete` 但没有输入生效 | Mac 未授权「输入监控」 | 系统设置→隐私与安全性→**输入监控**→勾选 `crosslink.app`（不是里面二进制），重勾后重启 .app |
| Mac 端日志 `CGEventTapCreate failed` / inject 报错 | TCC 授权缺失或 bundle id 变了 | 确认用 `.app bundle` 启动，且辅助功能+输入监控都授权 |
| Windows 端连接不上 | 防火墙拦截 | 用管理员 PowerShell 跑防火墙规则；或首次连接弹窗点「允许访问」(私有网络) |
| Win 鼠标移动但 Mac 光标不动、且 Mac 触控板也动 | 正常 | M2 默认双光标；Mac 本地输入与 Win 镜像输入叠加，两者都能动 Mac |
| 想要"切回控制 Win" | M2 当前是单向 | Win 自身物理输入始终直接控制 Win；如需 Mac 也控制 Win 需双向（见下「下一步」）|

## 安全

- `crosslink-server.key`（X25519 私钥）**不进 git、不打包分享**。
- 同一局域网内使用；跨公网需额外隧道（不在本期范围）。
- 别在同机同时跑 server+client 标准 inject 模式（本机回环风暴）。

## 下一步（可选）

- **双向控制**：让 Mac 也能控制 Win —— 需要两端都捕获+注入，或加"一键切换主控"热键。属于 M4 增强。
- **单光标无缝滑屏**：Win 鼠标光标从 Win 屏边缘滑入 Mac 屏、Win 鼠标一直驱动、Mac 只显示一个光标 —— 在 M2 上加 cursor 位置感知路由，属于 M4。
- **mDNS 自动发现**：Mac 自动发现 Win server，免输 IP。

# CrossLink M4 双机验证指南（Windows 服务端 + macOS 客户端 · 无缝单光标）

> 适用：**1 台 Windows + 1 台 Mac**。  
> 目标：验证 M4 无缝单光标——**Win 鼠标一直驱动一个逻辑光标**，从 Win 屏右缘滑出 → 落到 Mac 屏左缘并继续控制 Mac；推回 Mac 左缘 → 光标回到 Win。  
> 与 M3（指针所有权交接，交接后 Win 不再控制 Mac）不同，M4 是「真·单光标 KVM」。

---

## 0. M4 与 M2 / M3 的区别（先看这个）

| 模式 | Win 角色 | Mac 角色 | 光标表现 |
|------|----------|----------|----------|
| M2（默认） | master，转发键鼠 | slave，注入键鼠 | **双光标**；Win 光标撞到物理右缘后 Mac 光标冻结 |
| M3（`--switch`） | 初始 owner | 泊在接缝 | 单光标漫游；**交接后 Win 不再控制 Mac**（设计如此） |
| **M4（`--m4`）** | **master，唯一光标源** | **slave，只渲染光标位置 + 注入键鼠** | **单光标连续滑动**，Win 鼠标全程驱动 Mac |

M4 解决的问题：M2 鼠标到边缘就卡住、M3 交接后 Win 失去对 Mac 的控制。
M4 让 Win 鼠标像拖着一根「虚拟延长线」一样，把光标从 Win 屏无缝推到 Mac 屏。

> ⚠️ M4 假定物理布局：**Windows 在左，Mac 在右**（光标向右滑出 Win → 进入 Mac）。
> 目前 M4 固定「Win 右缘 = 进入 Mac」的几何，暂不支持 `--side` 反向布局。

---

## 1. 拓扑与默认布局

```
┌──────────────┐     滑出右缘      ┌──────────────┐
│  Windows     │ ───────────────▶ │    macOS     │
│  (server)    │   光标隐藏+warp   │   (client)   │
│  Win 鼠标驱动 │                  │  只渲染+注入  │
└──────────────┘                  └──────────────┘
   左                                 右
```

- Win 是 server（也是 master），Mac 是 client（slave）。
- 光标逻辑坐标：Win 屏 `[0, win_w)` → 越过 win_w 后映射到 Mac 屏 `[0, mac_w)`。
- y 方向按双屏高度线性映射（`map_y`），需等 Mac 的 `Hello` 携带 `screen_h` 到达后才能精确（到达前退化为恒等映射，短暂跳变属正常）。

---

## 2. Windows 端（server / master）

1. **构建**（沙箱已帮你 build 好 `target/x86_64-pc-windows-gnu/release/crosslink.exe`；真机自构建）：
   ```powershell
   cargo build --release --target x86_64-pc-windows-gnu
   ```
2. **放文件**（新建 `D:\crosslink\`，放两样）：
   - `crosslink.exe`
   - `crosslink-server.key`（仓库根目录已有，**必须同名同目录**——服务端身份私钥）
3. **放行防火墙**（管理员 PowerShell）：
   ```powershell
   New-NetFirewallRule -DisplayName "crosslink" -Direction Inbound -Program "D:\crosslink\crosslink.exe" -Action Allow -Profile Private
   ```
4. **启动 server**：
   ```powershell
   cd D:\crosslink
   .\crosslink.exe --server --m4 --name win
   ```
   终端会打印 `Server identity fingerprint ... AB:CD:EF:...`（M4 仍走 TOFU/加密，可记可不记）。

---

## 3. macOS 端（client / slave）

> M4 的 Mac 端**既要 warp 光标（`CGDisplay::warp_mouse_cursor_position`）又要 hide/unhide（`NSCursor`）还要注入键鼠事件**，
> 因此必须打 **`.app` bundle** 并授予 **辅助功能 + 输入监控** 两项 TCC（裸二进制在 macOS Tahoe 上拿不到稳定授权）。

1. **拉最新代码并构建**（Mac 上）：
   ```zsh
   cd ~/WorkBuddy/2026-08-07-15-05-29/crosslink   # 你的本地仓库路径
   git pull
   cargo build --release
   ```
2. **打包成 .app**（一次性；`com.crosslink.app` 固定，免重复授权）：
   ```zsh
   APP=/Users/xiaohua/Applications/crosslink.app
   mkdir -p $APP/Contents/MacOS $APP/Contents/Resources
   cp target/release/crosslink $APP/Contents/MacOS/crosslink
   cat > $APP/Contents/Info.plist <<'EOF'
   <?xml version="1.0" encoding="UTF-8"?>
   <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
   <plist version="1.0"><dict>
     <key>CFBundleName</key><string>crosslink</string>
     <key>CFBundleIdentifier</key><string>com.crosslink.app</string>
     <key>CFBundleVersion</key><string>1</string>
     <key>CFBundleExecutable</key><string>crosslink</string>
     <key>LSMinimumSystemVersion</key><string>11.0</string>
   </dict></plist>
   EOF
   # ad-hoc 签名（无需开发者证书）
   codesign --force --deep --sign - $APP
   ```
3. **授予 TCC**：系统设置 → 隐私与安全性 →
   - **辅助功能** → 勾选 `crosslink.app`
   - **输入监控** → 勾选 `crosslink.app`
   （两项都要；warp 光标靠输入监控，hide/unhide 靠辅助功能，键鼠注入也靠输入监控。）
4. **启动 client**（`<WIN_IP>` 换成 Windows 局域网 IP）：
   ```zsh
   $APP/Contents/MacOS/crosslink --client <WIN_IP> --m4 --name mac
   ```
   （也可复用 `crosslink-m3-client.sh <WIN_IP>`，把里面的 `--switch` 换成 `--m4`。）

---

## 4. 成功现象 + 日志标记

### 看现象（最关键）

- [ ] 初始：鼠标在 Windows 上正常用，Mac 屏上**没有** crosslink 的光标干扰。
- [ ] 把 Win 鼠标从 Windows 屏幕**右边缘**继续往右推 → Win 光标**消失**，Mac 屏**左边缘**出现光标并跟随移动；此时 Win 键鼠（键盘/点击）也作用于 Mac。
- [ ] 在 Mac 上继续往右移动 → 光标在 Mac 屏内连续移动（无跳变、无双光标）。
- [ ] 把光标推到 **Mac 屏左边缘**并继续往左 → 光标**回到 Windows 右边缘**，Win 重新可见并接管。
- [ ] 反复来回滑动，光标始终只有一个、连续无缝。

### 看日志

- 两端：`handshake complete` / `peer hello: ... screen WxH` / `heartbeat ack, rtt=...`
- Win 端（进入 Mac）：`m4: enter Mac region at win_y=... mapped mac_y=...`
- Win 端（回 Win）：`m4: return to Win region at win_y=...`
- Mac 端：`m4 inject: cursor shown on Mac at (x, y)` / `m4 inject: cursor hidden on Mac (in Win region)`

---

## 5. 排错

- **连不上**：同 M3——检查 Windows 防火墙是否放行 `crosslink.exe`（入站 / 专用），两端同一局域网、IP 正确。
- **Mac 光标不出现 / 不 warp**：多半是 TCC 没给全。**辅助功能 + 输入监控** 都要勾 `crosslink.app`；若失效重勾一次。
- **进入 Mac 后光标「瞬移」到左上角 (0,0)**：Mac 的 `Hello`（携带 screen 尺寸）还没到，y 映射退化为恒等。等 1 秒（握手后第一帧 `peer hello` 到达）即正常；若持续 (0,0)，检查 Mac 端是否真的收到 `peer hello`。
- **回不到 Win**：确保是把 Mac 光标推到**最左缘**后「继续往左推一下」（触发 `mac_cursor_x<=0 && dx<=0`）。在 Mac 屏中间往左只会让 Mac 光标左移，不会回 Win。
- **Win 上仍有残影/双光标**：M4 进入 Mac 时会 `ShowCursor(FALSE)` 隐藏 Win 光标；若异常退出导致 Win 光标一直隐藏，移动一下物理鼠标或重启 crosslink.exe 即可恢复（引用计数已经过循环调到目标状态）。

---

## 6. 安全提醒

- **不要在同一台机器上同时跑 server + client 做真实漫游**：两个进程抢同一个真实光标会打架。
- `crosslink-server.key` 是服务端私钥，**不要**进 git、不要明文打包；client 端不需要它。
- 传输已加密（X25519 + AES-GCM），首次 TOFU 信任；若要固定信任，client 加 `--fingerprint <server 指纹>`。
- `--m4` 与 `--switch` 互斥（main 已做参数校验，`--m4 --switch` 会直接报错退出）。
